<!DOCTYPE html>
<html lang="en">
<head>
    <title>Officer Dashboard - SAFEDEAL CONTRIBUTION</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <header class="header">
        <div class="header-content">
            <h1 class="site-title"><img src="/images/safedeallogo.png" alt="SAFEDEAL Logo" class="logo">SAFEDEAL CONTRIBUTION</h1>
            <nav class="header-nav">
                <a href="/officer-dashboard" class="nav-link">Dashboard</a>
                <span class="logout-wrapper">
                    <a href="/logout" class="logout-btn">🔒 Logout</a>
                </span>
            </nav>
        </div>
    </header>
    <div id="loading" class="loading-spinner" style="display:none;"></div>
    
    <main class="container">
        <div class="page-header">
            <h1>Welcome, <%= user.fullName %> (Loan Officer)</h1>
        </div>
        
        <p class="nav-p">
            <!-- This link allows the officer to go to any day to mark payments -->
            <a href="/daily/<%= today %>" class="dashboard-link">📅 View/Confirm Payments for <%= today %> (Go to any day)</a>
        </p>
        

        <section class="summary-section">
            <h2>Collection Summary & Loan Overview</h2>
            
            <!-- NEW: Disbursed and General Collected Cards -->
            <div class="kpi-grid">
                <div class="kpi-card disbursed-kpi">
                    <h4>💰 Total Disbursed</h4>
                    <p class="kpi-value">₦<%= totalLoansAmount.toFixed(0).toLocaleString() %></p>
                </div>
                <div class="kpi-card general-kpi">
                    <h4>📈 General Collected</h4>
                    <p class="kpi-value">₦<%= generalCollected.toFixed(0).toLocaleString() %></p>
                </div>
                <div class="kpi-card expected-kpi">
                    <h4>📊 Expected DUE Today</h4>
                    <p class="kpi-value">₦<%= todayExpected.toFixed(0).toLocaleString() %></p>
                </div>
                <div class="kpi-card collected-kpi">
                    <h4>✅ Collected TODAY</h4>
                    <p class="kpi-value">₦<%= todayCollected.toFixed(0).toLocaleString() %></p>
                </div>
            </div>
        </section>

        <% if (defaulters.length > 0) { %>
            <section class="defaulters-section">
                <h3 style="color: #c0392b; margin-top: 3rem;">⚠️ Borrowers Due & Unpaid Today (<%= today %>)</h3>
                <div class="table-wrapper">
                    <table class="data-table">
                        <thead>
                            <tr>
                                <th>Borrower</th>
                                <th>Expected</th>
                                <th>Collected</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% defaulters.forEach(d => { %>
                                <tr class="status-default">
                                    <td><%= d.borrower %></td>
                                    <td>₦<%= d.expected.toFixed(0).toLocaleString() %></td>
                                    <td>₦<%= d.collected.toFixed(0).toLocaleString() %></td>
                                </tr>
                            <% }); %>
                        </tbody>
                    </table>
                </div>
            </section>
        <% } else { %>
            <section class="success-section">
                <p style="color: green; margin-top: 3rem;">All borrowers due today have paid or are not due yet! 🎉</p>
            </section>
        <% } %>


        <p class="nav-p">
            
            <a href="/biweekly-flexible" class="tool-btn">📊 My Bi-Weekly Summary</a>
        </p>

        <div class="loan-chart-section">
        <h3>Weekly Repayment Progress</h3>
        <canvas id="loanChart"></canvas>
        </div>

        <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
        <script>
        const ctx = document.getElementById('loanChart');
        new Chart(ctx, {
            type: 'bar',
            data: {
            labels: ['Week 1', 'Week 2', 'Week 3'],
            datasets: [{
                label: 'Repayments',
                data: [5500, 6000, 7000],
                backgroundColor: ['#3498db', '#2ecc71', '#f1c40f']
            }]
            },
            options: {
            plugins: { legend: { display: true } },
            scales: { y: { beginAtZero: true } }
            }
        });
        </script>


    </main>

</body>
</html>