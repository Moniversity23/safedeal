<!DOCTYPE html>
<html>
<head>
    <title>Login | SAFEDEAL CONTRIBUTION</title>
    <link rel="stylesheet" href="/style.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/js/all.min.js" crossorigin="anonymous" referrerpolicy="no-referrer"></script>
    <style>
        .login-center {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 20px;
            background-color: #ecf0f1;
        }
        .login-card {
            max-width: 420px;
            width: 100%;
            padding: 2.5rem;
            background-color: #ffffff;
            border-radius: 15px;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
            text-align: center;
        }
        .login-card h1 {
            border-bottom: 2px solid #1abc9c;
            padding-bottom: 1rem;
            margin-bottom: 1.5rem;
            color: #2c3e50;
            font-size: 1.8rem;
        }
        .alert-error {
            color: #e74c3c;
            background-color: #f8d7da;
            padding: 12px 15px;
            border-radius: 6px;
            border: 1px solid #f5c6cb;
            text-align: left;
            font-size: 0.9rem;
            margin-bottom: 1rem;
        }
        form label {
            display: block;
            text-align: left;
            margin-top: 1rem;
            font-weight: 600;
            color: #2c3e50;
        }
        form input {
            width: 100%;
            padding: 10px;
            margin-top: 0.3rem;
            border: 1px solid #bdc3c7;
            border-radius: 6px;
        }
        .btn-primary {
            margin-top: 1.5rem;
            width: 100%;
            background-color: #1abc9c;
            color: white;
            border: none;
            padding: 10px;
            border-radius: 6px;
            cursor: pointer;
            font-weight: bold;
            transition: background-color 0.3s ease;
        }
        .btn-primary:hover {
            background-color: #16a085;
        }
    </style>
</head>
<body class="login-center">
    <div class="login-card">
        <h1><img src="/images/safedeallogo.png" alt="SAFEDEAL Logo" class="logo">SAFEDEAL CONTRIBUTION</h1>

        <% if (error) { %>
            <div class="alert-error">
                <i class="fas fa-exclamation-triangle"></i> <%= error %>
            </div>
        <% } %>

        <form action="/login" method="post">
            <label for="username"><i class="fas fa-user"></i> Username:</label>
            <input type="text" id="username" name="username" placeholder="Enter your username" required>

            <label for="password"><i class="fas fa-lock"></i> Password:</label>
            <input type="password" id="password" name="password" placeholder="Enter your password" required>

            <button type="submit" class="btn-primary"><i class="fas fa-sign-in-alt"></i> Log In</button>
        </form>
    </div>
</body>
</html>
